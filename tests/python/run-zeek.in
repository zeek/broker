#! /usr/bin/env bash

if [ -n "${BRO_EXECUTABLE}" ]; then
    exec "${BRO_EXECUTABLE}" $@

elif [ -n "${BRO_PATH_DEV}" ]; then
    source "${BRO_PATH_DEV}"
    exec zeek $@

else
    echo "No Zeek executable available" >&2
    exit 1
fi
